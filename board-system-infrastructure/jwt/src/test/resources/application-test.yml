spring:
  config:
    activate:
      on-profile: test

accessToken:
  rsaKeyPath:
    public: classpath:/rsa-key/access-token_public-key_jwt-test.pem
    private: classpath:/rsa-key/access-token_private-key_jwt-test.pem

refreshToken:
  rsaKeyPath:
    public: classpath:/rsa-key/refresh-token_public-key_jwt-test.pem
    private: classpath:/rsa-key/refresh-token_private-key_jwt-test.pem
