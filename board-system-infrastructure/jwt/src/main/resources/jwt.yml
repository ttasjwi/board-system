accessToken:
  rsaKeyPath:
    public: ${RSA_ACCESS_TOKEN_PUBLIC_KEY_PATH}
    private: ${RSA_ACCESS_TOKEN_PRIVATE_KEY_PATH}

refreshToken:
  rsaKeyPath:
    public: ${RSA_REFRESH_TOKEN_PUBLIC_KEY_PATH}
    private: ${RSA_REFRESH_TOKEN_PRIVATE_KEY_PATH}
