spring.application.name: board-system-test
spring:
  profiles:
    active: test
  config:
    import:
      - db-config.yml
      - security-config.yml
      - deploy-config.yml
      - message-config.yml

---
